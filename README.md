## Description

```
git clone https://github.com/philljj/vimrc.git
cd vimrc
cp vimrc.txt ~/.vimrc
```

Get legacy colorschemes while at it:
```
git clone https://github.com/philljj/colorschemes.git
cd colorschemes/legacy_colors/
mkdir -p ~/.vim/colors
cp elflord.vim ~/.vim/colors/
```
